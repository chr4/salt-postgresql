# TYPE  DATABASE  USER      ADDRESS  METHOD
local   all       postgres           trust

{%- for username, config in pillar['postgresql']['users']|dictsort %}
{{ config['type'] }} {{ config['database'] }} {{ username }} {{ config['address']|default('') }} {{ config['method'] }}
{% endfor -%}
